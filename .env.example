# Anthropic API Key (required for code translation)
ANTHROPIC_API_KEY=your-api-key-here

# Server port (optional, defaults to 3001)
PORT=3001

# Frontend API URL (optional, defaults to /api in development)
VITE_API_URL=/api
